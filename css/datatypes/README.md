# CSS.datatypes

This module contains structs and types to represent the types of CSS values and functions.
The purpose of this submodule is to provide a constant base of types and structures from
which CSS propreties can be represented.

Reference: [MDN](https://developer.mozilla.org/en-US/docs/Web/CSS) subsection `Reference -> types`.